import userService from '.';

describe('userService', () => {
  const user = userService(null);

  describe('getPosts()', () => {
    describe('when user wants to get all posts', () => {
      test('and succeed', async () => {});
      test('and it fails', async () => {});
    });
  });

  describe('getUsers()', () => {
    describe('when user wants to know info from all users', () => {
      test('and succeed', async () => {});
      test('and it fails', async () => {});
    });
  });
});
